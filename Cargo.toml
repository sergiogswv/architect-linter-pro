[package]
name = "architect-linter"
version = "3.1.0"
edition = "2021"
authors = ["Sergio Guadarrama"]
description = "Multi-language architecture linter with dynamic rules engine (TypeScript, JavaScript, Python, Go, PHP, Java)"
license = "MIT"
repository = "https://github.com/sergiogswv/architect-linter"
keywords = ["linter", "architecture", "multi-language", "code-quality", "tree-sitter"]
categories = ["development-tools"]

[dependencies]
# Forzamos una versión de serde que sabemos que tiene lo que swc busca
serde = { version = "=1.0.197", features = ["derive"] }
serde_json = "1.0.101"

# Usamos las versiones que no dan guerra con esa versión de serde
swc_common = { version = "0.33.0", features = ["concurrent"] }
swc_ecma_ast = "0.110.0"
swc_ecma_parser = "0.141.0"

walkdir = "2.5.0"
miette = { version = "7.2.0", features = ["fancy"] }
thiserror = "1.0"
rayon = "1.10.0"
dialoguer = "0.11.0"
indicatif = "0.18.0"

tokio = { version = "1.0", features = ["full"] } # Para manejar peticiones asíncronas
reqwest = { version = "0.11", features = ["json"] } # Cliente HTTP
async-trait = "0.1"
anyhow = "1.0"
console = "0.15"
notify = "6.1" # File system watcher for watch mode

# Tree-sitter for multi-language support
tree-sitter = "0.22"
tree-sitter-typescript = "0.21"
tree-sitter-python = "0.21"
tree-sitter-go = "0.21"
tree-sitter-rust = "0.21"
tree-sitter-java = "0.21"
tree-sitter-php = "0.22"